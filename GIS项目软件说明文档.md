**软件说明文档**

# 1 需求分析

耕地非农化是指耕地被转化为非农业用途（如建设用地、工矿用地等）的现象，是影响我国粮食安全和生态环境的重要问题。近年来，随着城镇化进程的加快，耕地非农化问题日益突出，特别是在经济发达地区和城市周边区域，耕地面积减少趋势明显。为了有效保护耕地资源、落实"严禁耕地非农化"政策要求，需要建立高效、准确的耕地非农化监测与分析系统。

传统的耕地监测手段主要依赖人工调查和简单的遥感影像目视解译，存在监测周期长、覆盖面有限、结果时效性差等不足。同时，对于耕地非农化的风险评估往往缺乏定量化分析手段，难以准确预测未来变化趋势。目前，虽然部分地区已引入遥感技术辅助耕地监测，但大多仅停留在简单的影像对比层面，缺少基于多年时序数据的深度分析能力，且没有将遥感特征分析与智能问答相结合的综合性解决方案。

随着遥感技术、地理信息系统（GIS）技术和人工智能技术的发展，利用多时相遥感影像实现耕地非农化的自动检测和风险评估成为可能。将WebGIS技术与AI大语言模型相结合，可以为用户提供直观的地图可视化和智能化的分析问答服务，大幅提升耕地保护工作的效率和科学性。

本项目面向武汉市耕地非农化变化检测需求，设计和开发了一套基于WebGIS的耕地非农化变化检测系统。该系统以武汉市2018—2022年多时相遥感影像为基础数据，支持在地图上浏览不同年份的遥感影像，提供时序影像播放功能，可以在地图上叠加显示地块矢量数据和风险等级标注。用户可以点击地图上的地块，查看其非农化风险预测结果、特征贡献分析（雷达图与差值图），并通过集成的AI智能问答功能与系统交互，获取针对特定地块的深入分析。系统前端采用React + MapLibre GL技术栈构建，后端基于Python FastAPI框架，利用rio-tiler实现动态瓦片服务，并接入DeepSeek大语言模型提供智能问答能力。

# 2 软件总体设计

## 2.1 开发环境

操作系统：Windows 10 64位

后端框架：Python 3 + FastAPI + Uvicorn

前端框架：React 19 + Vite 7

地图引擎：MapLibre GL JS

图表库：ECharts 6

IDE：Visual Studio Code

开发语言：Python（后端）、JavaScript/JSX（前端）

## 2.2 系统架构

系统采用前后端分离的B/S架构设计。前端基于React框架构建单页面应用（SPA），使用MapLibre GL JS作为地图渲染引擎，通过Vite开发服务器的代理功能与后端API通信。后端基于Python FastAPI框架，提供RESTful API服务，包括地块矢量数据接口、遥感影像动态瓦片服务、预测分析接口和AI问答接口。后端从本地数据文件（GeoJSON、Excel、TIF遥感影像）中加载数据，并集成DeepSeek大语言模型API和Tavily联网搜索API，为智能问答功能提供支撑。前端通过Zustand进行全局状态管理，实现地图交互、地块选择、分析面板和问答面板之间的数据联动。

<!-- 插图位置 -->
<!-- 图1：系统总体架构设计图 -->
<!-- 说明：该图应展示系统的整体B/S架构，分为三层：(1) 前端展示层——包含React SPA、MapLibre GL地图引擎、ECharts图表组件、智能问答面板；(2) 后端服务层——包含FastAPI应用、动态瓦片服务(rio-tiler)、预测分析引擎(EvaluationModel)、AI中转服务(DeepSeek + Tavily)；(3) 数据层——包含遥感影像TIF文件(2018-2022)、地块矢量GeoJSON、预测结果Excel、特征重要性Excel。三层之间用箭头表示数据流向：前端通过HTTP/REST API与后端通信，后端从数据层读取本地文件并调用外部AI API。 -->

图1 系统总体架构设计图

## 2.3 主要功能模块

**（1）遥感影像时序浏览模块**

提供武汉市2018—2022年多时相遥感影像的地图展示。系统通过后端动态瓦片服务将TIF格式遥感影像以Web瓦片形式加载到MapLibre GL地图中，支持地图的缩放、平移、旋转等交互操作。同时提供时间轴滑块控件，用户可以拖动滑块切换查看不同年份的遥感影像，或点击播放按钮自动播放影像时序变化。

**（2）地块非农化风险预测模块**

在地图上叠加显示地块矢量边界，并根据2022年预测结果以不同颜色标注风险等级（0-3级，对应低风险/中低风险/中高风险/高风险）。用户点击任一地块后，系统弹出浮动分析面板，展示该地块的风险等级、置信度，以及2020—2023年多年度的非农化特征贡献雷达图和年度差值柱状图，帮助用户理解驱动非农化变化的关键因子。

**（3）非农化综合评价模块**

提供基于规则和加权的非农化风险综合评分功能。评价模型（EvaluationModel）从当前风险等级、风险持续性、长期趋势、近期变化、特征强度、波动惩罚六个维度进行量化评分，生成综合评分（0-100分）和评分映射等级。评价结果以浮动卡片形式展示，包含评分环、风险等级标签、置信度进度条、评分拆解明细和Top10贡献特征柱状图。

**（4）AI智能问答模块**

在侧边栏提供智能问答面板，用户可以针对选中地块提出自然语言问题。系统将地块上下文数据与用户问题一并发送至后端，后端通过DeepSeek大语言模型进行推理分析，以流式方式返回包含思考过程和分析结论的回答。对于涉及外部事件、政策等的问题，系统自动通过Tavily API联网搜索，将检索结果融入AI回答中。同时支持对话历史记录和回溯查看功能。

**（5）变化检测热力图模块**

提供基年与目标年之间的变化检测可视化功能。系统后端对两个年份的遥感影像进行归一化差值计算，提取非农化变化强度信息，以红色热力图瓦片的形式叠加在地图上，直观呈现耕地非农化变化的空间分布。

**（6）视图模式切换模块**

提供"地块分析模式"和"影像时序模式"两种视图模式的切换功能。在地块分析模式下，地图上叠加地块矢量数据和风险标注，用户可以点击查看地块详情；在影像时序模式下，地图着重展示遥感影像和时间轴播放功能，便于用户对比不同年份的影像变化。

<!-- 插图位置 -->
<!-- 图2：系统功能模块逻辑框图 -->
<!-- 说明：该图应展示上述6个功能模块之间的逻辑关系。中心为地图主界面(MapBoard)，由地图引擎连出：(1) 遥感影像时序浏览模块(含TimelineSlider时间轴)；(2) 地块非农化风险预测模块(含ParcelAnalysisFloat浮动面板)；(3) 非农化综合评价模块(含OverlayCard评价卡片)；(4) AI智能问答模块(含AnalysisPanel侧边栏 → ChatBox/HistoryPanel)；(5) 变化检测热力图模块(与遥感模块联动)；(6) 视图模式切换模块(FloatingChat切换按钮)。各模块之间通过Zustand全局状态(useStore)进行数据联动，用虚线箭头表示。 -->

图2 系统功能模块逻辑框图

## 2.4 接口设计

**GET /api/parcels**

\[功能\] 获取所有地块的GeoJSON矢量数据，每个地块附带风险等级、风险标签和置信度属性

\[参数说明\] 无

\[返回值说明\] GeoJSON FeatureCollection，每个Feature的properties中包含OBJECTID、risk_level（0-3）、risk_label（低风险/Low/Mid/High）、risk_confidence

**GET /api/predict/{parcel_id}**

\[功能\] 获取指定地块的非农化风险预测详情

\[参数说明\] parcel_id：地块OBJECTID

\[返回值说明\] JSON对象，包含parcel_id、prediction（等级level、概率分布probabilities、置信度confidence）、explainability（特征贡献features）、real_data（地块名称name、国别gb、历年等级yearly_levels）

**GET /api/predict_history/{parcel_id}**

\[功能\] 获取指定地块的多年度特征重要性数据，用于绘制雷达图和差值图

\[参数说明\] parcel_id：地块OBJECTID；top_k：返回Top特征数量（可选，支持5或10，默认10）

\[返回值说明\] JSON对象，包含parcel_id、yearly_features（各年度特征值字典）、yearly_classes（各年度类别标签）、feature_catalog（特征名录）、top_k_used

**GET /api/tiles/{year}/{z}/{x}/{y}**

\[功能\] 获取指定年份、指定层级和坐标的遥感影像PNG瓦片

\[参数说明\] year：年份（2018-2022）；z：缩放级别；x、y：瓦片坐标

\[返回值说明\] PNG格式影像瓦片（image/png），支持缓存控制

**GET /api/change_tiles/{base_year}/{current_year}/{z}/{x}/{y}**

\[功能\] 获取基年与目标年之间的变化检测热力图PNG瓦片

\[参数说明\] base_year：基年；current_year：目标年；z、x、y：瓦片坐标

\[返回值说明\] PNG格式变化热力图瓦片，红色表示非农化变化区域

**POST /api/chat**

\[功能\] 与AI进行智能对话，传入用户问题和地块上下文数据，返回流式AI分析结果

\[参数说明\] JSON请求体，包含message（用户问题字符串）和context（当前选中地块上下文对象）

\[返回值说明\] 流式文本响应（text/plain），包含\<thinking\>思考过程\</thinking\>标签和分析结论；对于评价请求额外返回\<ui_eval\>JSON\</ui_eval\>标签触发前端弹窗

**GET /api/forecast/{parcel_id}**

\[功能\] 获取指定地块的未来非农化预测（当前为占位接口）

\[参数说明\] parcel_id：地块OBJECTID

\[返回值说明\] JSON对象，包含预测模型信息和占位说明

<!-- 插图位置 -->
<!-- 图3：接口逻辑框图 -->
<!-- 说明：该图应展示前端与后端之间的API接口调用关系。左侧为前端模块（MapBoard地图组件、ParcelAnalysisFloat分析面板、OverlayCard评价卡片、ChatBox聊天框），右侧为后端API端点（/api/parcels、/api/predict/{id}、/api/predict_history/{id}、/api/tiles/{year}/{z}/{x}/{y}、/api/change_tiles/...、/api/chat、/api/forecast/{id}）。用带标注的箭头连接前端模块与其调用的API端点，标注请求方法(GET/POST)。后端API端点进一步连接到数据源（本地GeoJSON、Excel、TIF文件）和外部服务（DeepSeek API、Tavily API）。 -->

图3 接口逻辑框图

## 2.5 非农化评价模型设计

系统内置了一个轻量级可解释评价模型（EvaluationModel），采用基于规则和加权的方式对地块非农化风险进行综合评分。模型由以下六个评分维度构成：

- **当前风险（权重40%）**：以最新年度（2022年）的预测等级为基础，归一化到\[0,1\]后加权计分，反映地块当前的风险状态。
- **风险持续性（权重20%）**：计算近4年（2019-2022）等级的平均值，体现"持续偏高"的程度，惩罚长期处于高风险的地块。
- **长期趋势（权重15%）**：计算2022年相对2019年的净变化，仅奖励上升趋势（下降截断为0），捕捉风险的长期走向。
- **近期变化（权重10%）**：计算2022年相对2021年的变化，反映短期内的风险抬升。
- **特征强度（权重15%）**：取Top5特征贡献均值，衡量关键驱动因子的强弱程度。
- **波动惩罚（权重10%）**：计算历史等级的标准差，波动越大惩罚越重，鼓励稳定的评估结果。

最终综合评分截断到\[0, 100\]范围，并映射为离散风险等级：评分<30为0级（低风险）、30-50为1级（中低风险）、50-70为2级（中高风险）、≥70为3级（高风险）。

<!-- 插图位置 -->
<!-- 图4：非农化评价模型流程图 -->
<!-- 说明：该图应展示EvaluationModel的评分计算流程。输入：2022年风险等级(level)、2019-2022年历年等级(yearly_levels)、Top5/Top10特征值(features)。六个计算分支：当前风险(×40)→current_score、持续性(×20)→persistence_score、长期趋势(×15)→trend_score、近期变化(×10)→recent_score、特征强度(×15)→feature_score、波动惩罚(×10)→penalty。六个分支汇合求和→综合评分score(0-100)→映射为离散等级(0/1/2/3)。输出：评分、等级、分项拆解、Top特征列表。 -->

图4 非农化评价模型流程图

# 3 功能实现

## 3.1 软件准备

**（1）安装Python环境**

安装Python 3.8及以上版本，建议使用Anaconda或venv创建独立虚拟环境。在项目根目录下运行 `pip install -r requirements.txt` 安装后端所需的全部依赖包，包括FastAPI、uvicorn、rio-tiler、rasterio、numpy、pandas等。

**（2）安装Node.js环境**

从Node.js官网下载安装LTS版本（建议v18+）。安装完成后，进入frontend目录，运行 `npm install` 安装前端依赖包，包括React、MapLibre GL、ECharts、Axios、Zustand等。

**（3）配置环境变量**

在frontend目录下的.env文件中配置后端API地址（默认为 `VITE_API_BASE_URL=http://127.0.0.1:8000`）。后端的DeepSeek API密钥和Tavily API密钥可通过环境变量或在代码中直接配置。

**（4）准备数据文件**

在backend/data目录下放置以下数据文件：地块矢量GeoJSON文件（test_4326.json）、武汉2018-2022遥感影像文件夹（包含WH2018_1.tif至WH2022_1.tif）、预测结果Excel文件（预测结果.xlsx）、特征重要性文件夹（包含各年度Excel文件）。

<!-- 插图位置 -->
<!-- 图5：项目目录结构截图 -->
<!-- 说明：该截图应展示项目的完整文件目录树形结构，包括根目录下的backend/、frontend/、scripts/、start.bat、requirements.txt等，以及backend/下的main.py、preprocess.py、data/子目录，frontend/下的src/、src/components/、src/store/、package.json、vite.config.js等。可使用文件资源管理器或IDE侧边栏截图。 -->

图5 项目目录结构

## 3.2 实现地图交互功能

**（1）初始化MapLibre GL地图**

在React组件MapBoard中，使用MapLibre GL JS初始化地图实例，设置初始中心坐标为武汉市区域（\[113.84, 30.18\]），初始缩放级别为12级。地图底图采用OpenStreetMap瓦片服务，同时支持通过环境变量配置自定义地图样式。

**（2）加载遥感影像瓦片图层**

在地图加载完成后，通过addSource方法添加raster类型的遥感影像瓦片源，瓦片URL指向后端动态瓦片API `/api/tiles/{year}/{z}/{x}/{y}`。通过addLayer方法将瓦片源渲染为地图图层，并根据用户选择的年份动态更新瓦片URL参数。

**（3）加载地块矢量图层**

通过Axios请求后端 `/api/parcels` 接口获取GeoJSON数据，以GeoJSON source方式加载到地图中。使用fill类型图层渲染地块多边形，根据risk_level属性以不同颜色填充（绿色→黄色→橙色→红色），并添加line类型图层绘制地块边界。

**（4）实现地块点击交互**

为地块矢量图层绑定click事件监听器，当用户点击某一地块时，获取地块的OBJECTID，向后端 `/api/predict/{parcel_id}` 接口请求预测详情，将返回结果通过Zustand状态管理存入全局store，触发ParcelAnalysisFloat分析面板的显示。同时高亮选中地块的边界。

<!-- 插图位置 -->
<!-- 图6：地图主界面截图 -->
<!-- 说明：该截图应展示系统运行时的地图主界面，包括：顶部标题栏（"🌾 武汉耕地非农化变化检测系统 2018-2022"）、左侧地图区域（显示遥感影像底图和彩色地块矢量叠加）、右侧智能问答侧边栏、底部时间轴滑块控件。地图上应能看到不同颜色的地块多边形（代表不同风险等级）。 -->

图6 地图主界面

## 3.3 实现时序影像播放功能

**（1）时间轴滑块组件**

在地图底部叠加TimelineSlider组件，包含一个播放/暂停按钮和一个范围滑块（2018-2022年）。滑块下方标注每个年份刻度，当前选中年份以高亮样式突出显示。

**（2）自动播放逻辑**

点击播放按钮后，系统启动定时器，按固定间隔（如2秒）自动递增年份，切换地图上的遥感影像瓦片图层。当年份到达2022年后自动循环回2018年继续播放。拖动滑块手动选择年份时自动暂停播放。

**（3）变化检测图层联动**

在影像时序模式下，除显示当前年份的遥感影像外，系统同时加载基年（2018年）与当前年份之间的变化检测热力图层，以半透明红色覆盖方式叠加在地图上，直观展示非农化变化区域的空间分布。

<!-- 插图位置 -->
<!-- 图7：时序影像播放界面截图 -->
<!-- 说明：该截图应展示影像时序模式下的界面，重点展示底部时间轴控件（包含播放按钮、年份滑块、当前年份数字）和地图上的遥感影像。可截取两张不同年份的对比截图（如2018年和2022年），展示同一区域影像的变化。 -->

图7 时序影像播放界面

## 3.4 实现地块分析面板

**（1）浮动分析面板**

用户点击地块后弹出ParcelAnalysisFloat浮动面板，面板采用可拖动、可缩放的设计。标题栏显示地块ID，支持鼠标拖动调整位置，右下角支持拉伸调整大小。

**（2）风险等级展示**

面板顶部以卡片形式展示预测风险等级和置信度，高风险（≥2级）使用红色系配色，低风险使用绿色系配色，一目了然。

**（3）多年度雷达图**

面板中部以2×2网格布局展示2020、2021、2022、2023四个年度的特征贡献雷达图（基于ECharts实现）。每个雷达图以多边形面积展示Top K个特征维度的贡献值，支持在Top5和Top10之间切换。图例中显示该年度的类别标签（None/Low/Mid/High）。

**（4）年度差值柱状图**

面板底部展示2023年与2020年之间的Top6特征差值柱状图，正值（绿色）表示该特征贡献增大，负值（红色）表示减小，帮助用户快速识别变化最显著的特征维度。

**（5）特征说明抽屉**

点击"特征说明"按钮，在面板右侧弹出特征说明抽屉，列出Top K个特征的名称和语义说明（如"植被覆盖相关特征"、"纹理差异特征"、"光谱波段特征"等），辅助用户理解各特征维度的含义。

<!-- 插图位置 -->
<!-- 图8：地块分析面板截图 -->
<!-- 说明：该截图应展示点击某个地块后弹出的ParcelAnalysisFloat浮动面板完整界面，包含：(1) 标题栏（"⚙ 地块分析 ID: xxx"）；(2) 风险等级卡片（如"预测风险：3级"、"置信度: 85%"）；(3) 四个年度雷达图（2020-2023年）；(4) 底部的年度差值柱状图。最好同时展示地图上高亮选中的地块。 -->

图8 地块分析面板

## 3.5 实现非农化综合评价功能

**（1）触发评价**

用户在智能问答面板中输入包含"非农化评价"关键词的问题（如"请对该地块进行非农化评价"），系统识别到评价意图后，在后端直接调用EvaluationModel进行本地计算，无需等待外部AI接口响应。

**（2）流式输出思考过程**

评价计算完成后，系统以流式方式先输出\<thinking\>标签包裹的详细推理过程（包括各维度的量化计算步骤），前端以打字机效果逐字展示思考过程，随后自动折叠。

**（3）评价结果卡片弹窗**

思考过程输出完毕后，系统发送\<ui_eval\>标签触发前端弹出OverlayCard评价结果卡片，卡片中包含：评分环（SVG圆环进度条）、风险等级与置信度、评分拆解六维明细、历史类别标签、Top10贡献特征柱状图。卡片同样支持拖动和缩放操作。

<!-- 插图位置 -->
<!-- 图9：非农化综合评价结果截图 -->
<!-- 说明：该截图应展示OverlayCard评价结果卡片的完整界面，包含：(1) 标题栏（"📊 非农化评价 ID: xxx"）；(2) 评分环（如显示综合评分68/100）；(3) 风险等级和置信度卡片并排展示；(4) 评分拆解六维网格（当前风险、风险持续性、长期趋势、近期变化、特征强度、波动惩罚）；(5) 历史类别标签（2020年/2023年分类结果）；(6) 贡献特征Top10横向条形图。 -->

图9 非农化综合评价结果

## 3.6 实现AI智能问答功能

**（1）问答面板布局**

右侧边栏AnalysisPanel包含标题栏（"💬 智能问答"）、历史对话面板（HistoryPanel）和聊天输入框（ChatBox）。输入框支持多行输入，按Enter或点击发送按钮提交问题。

**（2）流式对话交互**

用户发送问题后，前端通过fetch API以POST方式调用 `/api/chat` 接口，使用ReadableStream逐块读取服务端的流式响应。AI的思考过程（\<thinking\>标签内容）以打字机效果逐字展示，完成后自动收起为可展开的折叠块；分析结论直接展示在对话气泡中。

**（3）联网搜索增强**

后端在处理用户问题时，首先通过一次轻量级LLM调用判断是否需要联网搜索（如涉及政策、新闻、规划等外部信息）。若需要联网，则自动调用Tavily搜索API获取相关网页摘要，将搜索结果注入AI提示词中，使AI回答能够结合最新的外部信息。

**（4）对话历史管理**

当用户切换选中不同地块时，当前对话自动保存至历史记录。历史对话面板以列表形式展示，每条记录显示地块ID后四位、首条用户消息摘要和时间戳，点击可展开查看完整对话内容。历史记录通过Zustand的persist中间件持久化到浏览器localStorage中。

<!-- 插图位置 -->
<!-- 图10：AI智能问答界面截图 -->
<!-- 说明：该截图应展示右侧智能问答面板的完整交互过程，包含：(1) 顶部"💬 智能问答"标题栏；(2) 历史对话列表（显示若干条历史记录，含地块ID和消息摘要）；(3) 一轮完整的对话——用户提问（蓝色气泡）和AI回答（灰色气泡，其中包含一个可展开的"思考过程"折叠块和正文分析结论）；(4) 底部输入框和发送按钮。 -->

图10 AI智能问答界面

## 3.7 遥感影像预处理

**（1）预处理脚本**

backend/preprocess.py提供遥感影像的预处理功能，根据地块GeoJSON矢量边界对原始TIF遥感影像进行裁剪。脚本自动处理坐标系转换（从WGS84 EPSG:4326到栅格影像坐标系），使用rasterio的mask功能对影像进行空间裁剪，生成裁剪后的TIF文件以减小数据体积。

**（2）模拟数据生成**

scripts/generate_mock_map.py提供模拟地块数据生成功能，以武汉附近坐标为起点，生成10×10的网格地块矢量数据（GeoJSON格式），每个地块附带随机面积和土地类型属性，用于开发测试阶段的数据准备。

# 4 关键技术

## 4.1 动态瓦片服务

系统通过rio-tiler库实现遥感影像的动态瓦片服务。rio-tiler基于rasterio读取云优化GeoTIFF（COG）格式的遥感影像，根据前端请求的缩放级别和瓦片坐标，动态裁剪和渲染对应区域的影像瓦片。为提升性能，系统对瓦片渲染结果进行LRU缓存（缓存容量20000个瓦片），并对影像的归一化参数进行预计算和缓存。影像归一化采用2%-98%百分位拉伸，保证影像在Web端的视觉效果。

## 4.2 变化检测算法

变化检测基于多时相遥感影像的光谱差异分析。系统对基年和目标年的影像分别进行百分位归一化后，计算非农化指数（近红外+红色波段减去绿色波段的差值），然后求两个年份之间的非农化指数差值（delta）。当差值超过阈值（0.08）时，判定为发生了非农化变化，变化强度通过线性映射到\[0, 1\]范围后以红色渐变方式进行可视化。

## 4.3 前端状态管理

系统使用Zustand作为前端全局状态管理方案。Zustand相比Redux更加轻量，无需繁琐的action/reducer模板代码。通过一个统一的useStore hook管理以下全局状态：选中地块数据、聊天历史记录、视图模式、评价叠层数据和位置信息、地块点击坐标等。聊天历史记录通过persist中间件自动持久化到localStorage，确保页面刷新后对话不丢失。

## 4.4 AI推理与流式传输

系统集成DeepSeek大语言模型API进行智能问答。后端通过精心设计的提示词工程，将AI设定为"非农化风险分析引擎的核心推理模块"角色，约束其以专业、简洁的方式输出分析结论。响应文本通过StreamingResponse以5字符/块、40毫秒/块的节奏进行流式传输，前端配合打字机效果（3字符/帧、16毫秒/帧）实现平滑的逐字显示效果。

# 5 软件系统亮点

## 5.1 多源数据融合

系统将多时相遥感影像、地块矢量数据、机器学习预测结果、特征重要性数据和AI大语言模型能力融合于一体，构建了从"数据展示→风险评估→智能分析"的完整闭环。用户无需在多个工具之间切换，即可在一个界面内完成从遥感影像浏览到深度分析的全部工作流程。

## 5.2 可解释性

系统不仅提供风险预测结果，还通过多年度特征贡献雷达图、年度差值柱状图、综合评分拆解六维明细、Top K贡献特征排行等方式，帮助用户深入理解预测结果背后的驱动因子和演变逻辑，增强决策的可信度和透明度。

## 5.3 智能化交互

系统集成AI大语言模型，支持用户以自然语言方式对地块数据进行提问和分析。AI的思考过程可视化展示，流式输出的打字机效果提升了交互体验。同时，联网搜索增强功能使AI回答能够融合最新的政策、规划等外部信息，提供更全面的分析视角。

## 5.4 易用性

系统界面设计简洁直观，采用深蓝色渐变主题，视觉层次分明。地块分析面板和评价卡片均支持拖动和缩放，方便用户根据自身需求调整布局。时间轴滑块控件操作便捷，风险等级以直观的颜色编码呈现，降低了用户的学习成本。

## 5.5 扩展性

系统采用前后端分离架构，前端和后端可以独立部署和扩展。后端预留了未来预测接口（/api/forecast），可在接入训练模型后实现未来1-3年的风险预测。前端的组件化设计（MapBoard、AnalysisPanel、ParcelAnalysisFloat、OverlayCard等）便于新增功能模块。同时，系统可通过修改数据文件轻松适配其他城市或区域的耕地监测需求。
